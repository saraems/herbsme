export interface IProduct { 
    name: string;
    price: number;
    categoty: string;
    image: string,
    origin: string,
    harvested: number,
    healingProperties: string,
    description: string,
}